on: [push]

env:
    GH_TOKEN: ${{ github.token }}

jobs:
  check-coding-style:
    runs-on: ubuntu-latest
    name: Check Coding Style
    steps:
      - uses: actions/checkout@v5
      - uses: ./
        with:
          path: tests/
      - name: Independant cs2 run
        run: cs2
